#version 430 //GLSL version your computer supports

out vec4 daColor;

in vec2 UV;
in vec3 normalWorld;
in vec3 vertexPositionWorld;



uniform sampler2D texture0;
uniform sampler2D myTextureSampler_0;
uniform sampler2D myTextureSampler_1;

uniform vec3 ambientLight;

void main()
{
	vec3 normal = normalize(normalWorld);

	normal = texture(myTextureSampler_1, UV).rgb;
	normal = normalize(normal*2 - 1.0);

	normalw = texture(texture0, UV).rgb;
	normalw = normalize(normalw*2 -1.0);
	daColor = vec4((texture(texture0, UV).rgb + normal)*ambientLight, 1.0);

}